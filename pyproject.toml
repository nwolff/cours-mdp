[project]
name = "cours-mdp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "google-cloud-firestore>=2.27.0",
    "gunicorn>=25.3.0",
    "segno>=1.6.6",
]

[dependency-groups]
dev = [
    "mypy>=1.20.1",
    "ruff>=0.15.10",
]
